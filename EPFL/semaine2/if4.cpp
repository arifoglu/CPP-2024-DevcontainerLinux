#include <iostream>
using namespace std;

int main(){
    int n,m;
    cout << "enter a two number a with gap :";
    cin  >> n >> m;
    
    (n > 5 or m > 10) ? cout << "first is greater than 5 or second greater than 10" : cout << 0 <<endl;

    //(n > 5 || m > 10) ? cout << "first is greater than 5 or second greater than 10" : cout << 0 <<endl;
    
    return 0;
}